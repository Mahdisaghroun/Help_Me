import { CallDirective } from './call.directive';

describe('CallDirective', () => {
  it('should create an instance', () => {
    const directive = new CallDirective();
    expect(directive).toBeTruthy();
  });
});
